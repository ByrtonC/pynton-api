export enum CodeError {
    'existsName' = 1,
    'existsUsername' = 2,
    'passwordInvalid' = 3,
    'usernameInvalid' = 4,
    'existsID' = 5,
}

export enum AccountType {
    'owner' = 'owner',
    'member' = 'member',
}
